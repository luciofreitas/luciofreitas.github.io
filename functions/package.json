{
  "name": "firebase-verify-user",
  "version": "1.0.0",
  "description": "Firebase Cloud Function to verify Firebase ID tokens and upsert users into Supabase",
  "main": "index.js",
  "engines": {
    "node": "18"
  },
  "scripts": {
    "start": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions:firebaseVerify"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^4.0.0",
    "@supabase/supabase-js": "^2.31.0",
    "cors": "^2.8.5"
  }
}
