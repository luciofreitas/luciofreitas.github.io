# Example backend .env (do NOT commit secrets)
# Copy to backend/.env and fill real values for deployment/local testing

DATABASE_URL=postgresql://parts:parts_pass@localhost:5432/partsdb
RENDER_API_BASE=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=
PORT=3001
